name: Build And Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: "kz14"
          password: "3Mhnpv3yq3@doc"

      - name: Build Docker image
        run: docker build -t kz14/myapp:${{ github.sha }} .

      - name: Push Docker image
        run: docker push kz14/myapp:${{ github.sha }}

      - name: Build Docker image
        run: docker build -t kz14/myapp:${{ github.sha }} -f week-3/day-3/Dockerfile week-3/day-3/
